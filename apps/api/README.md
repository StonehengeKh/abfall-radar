# API

Reserved for the Node.js API that will normalize municipal providers, expose versioned contracts,
and support cross-device features.

The API framework and persistence dependencies are selected in an architecture decision record
when the first backend task is approved.
