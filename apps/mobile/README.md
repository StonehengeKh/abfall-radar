# Mobile application

Reserved for a future Expo and React Native application.

Mobile UI shares domain rules and API contracts, not DOM components. Expo dependencies are added
only when the mobile product stage starts.
