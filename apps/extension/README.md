# Browser extension

The browser extension is the first AbfallRadar product surface. It owns browser lifecycle,
permissions, storage, alarms, notifications, and popup composition.

Domain rules, municipal data providers, and reusable visual primitives must be imported from
workspace packages rather than reimplemented here.
