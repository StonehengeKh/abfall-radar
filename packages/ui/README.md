# UI

Shared DOM-based React primitives, semantic design tokens, icons, and baseline styles for the
browser extension and web application.

This package must remain responsive and host-agnostic. React Native components do not belong here.
